import { mongoConnectionString } from './Constants'
import User from '../models/User'
import Session from '../models/Session'
import Recipe from '../models/Recipe'

export default class Database {
  static start() {

  }

  // static getUser({ username, email }: { username?: string, email?: string }): User {

  // }

  // static getSession(sessionID: string): Session {
    
  // }

  // static getRecipes(searchTerm?: string): Recipe[] {

  // }

  // static getRecipe(recipeID): Recipe {

  // }
}

Database.start()

